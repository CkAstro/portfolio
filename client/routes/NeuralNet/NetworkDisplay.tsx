import css from './NeuralNet.module.scss';

type Props = {
   draw: any;
   mask: Uint8Array;
   setOutput: any;
};

const NetworkDisplay = ({ draw, mask, setOutput }: Props): JSX.Element => {
   // const [detworkData, setNetworkData] = useState<any>(null);

   return <div />;
};

export default NetworkDisplay;
